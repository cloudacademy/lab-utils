## Dialogflow Inline Editor Updated Files

This folder contains updated `index.js` and `package.json` files for the Dialogflow ES Inline Editor. They replace the deprecated default code that targets Node.js 10 with a Cloud Functions 1st gen compatible version using Node.js 22.